{
  "name": "mollire",
  "version": "0.1.0",
  "private": true,
  "description": "Mini-Vercel: multi-tenant deploy service for static frontend builds. Each app under apps/ owns its own package.json, node_modules, and .env — this root has no dependencies of its own, just passthrough scripts.",
  "scripts": {
    "dev:api": "npm --prefix apps/api run start:dev",
    "dev:web": "npm --prefix apps/web run dev",
    "build:api": "npm --prefix apps/api run build",
    "build:web": "npm --prefix apps/web run build",
    "prisma:generate": "npm --prefix apps/api run prisma:generate",
    "prisma:migrate": "npm --prefix apps/api run prisma:migrate"
  },
  "engines": {
    "node": ">=22"
  }
}
